{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python -c \"import sys; sys.exit('No default service: set this service\\'s Config-as-code path to /railway/<job>.json — see README > Deploying to Railway.')\"",
    "restartPolicyType": "NEVER"
  }
}
